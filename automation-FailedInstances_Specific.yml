args:
- name: instance_name
comment: Executes a test for all integration instances available and returns detailed
  information about succeeded and failed integration instances.
commonfields:
  id: e2909797-da52-4e8f-8cc1-c5aef61e8838
  version: -1
contentitemexportablefields:
  contentitemfields:
    definitionid: ""
    fromServerVersion: ""
    itemVersion: ""
    packID: ""
    packName: ""
    prevname: ""
    propagationLabels:
    - all
    toServerVersion: ""
enabled: true
engineinfo: {}
mainengineinfo: {}
name: FailedInstances_Specific
outputs:
- contextPath: FailedInstances.instance
  description: The name of the failed integration instance.
  type: string
- contextPath: FailedInstances.brand
  description: The brand name of the failed integration instance.
  type: string
- contextPath: FailedInstances.category
  description: The category of the failed integration instance.
  type: string
- contextPath: FailedInstances.information
  description: Error information about the failed integration instance.
  type: string
- contextPath: FailedInstances.status
  description: The status of the instance.
  type: string
- contextPath: InstancesCount.FailedCount
  description: The number of failed instances.
  type: string
- contextPath: InstancesCount.SuccessCount
  description: The number of working instances.
  type: string
- contextPath: InstancesCount.TotalCount
  description: The number of total enabled instances.
  type: string
- contextPath: SuccessInstances.brand
  description: The brand name of the successful integration instance.
  type: string
- contextPath: SuccessInstances.category
  description: The category of the successful integration instance.
  type: string
- contextPath: SuccessInstances.information
  description: Information that the integration instance was succesful.
  type: string
- contextPath: SuccessInstances.instance
  description: The name of the successful integration instance.
  type: string
- contextPath: SuccessInstances.status
  description: The status of the instance.
  type: string
pswd: ""
runas: DBotWeakRole
runonce: false
script: |-
  // pack version: 1.21.4
  var INTERNAL_MODULES_BRANDS = ['Scripts', 'Builtin', 'testmodule'];

  var failedInstances = [];
  var all = getModules();
  var countFailed = 0;
  var countSuccess = 0;
  var instances = [];

  // ✅ Correct way to get args in JS
  var targetInstance = args.instance_name;

  const brandConfig = {
      "ServiceNow v2": {
          command: "servicenow-oauth-test",
          message: "Test button cannot be used"
      },
      "ServiceNow CMDB": {
          command: "servicenow-cmdb-oauth-test",
          message: "Test button cannot be used"
      },
      "Microsoft Graph Mail Single User": {
          command: "msgraph-mail-test",
          message: "Please use !msgraph-mail-test instead"
      },
      "Microsoft Graph API": {
          command: "msgraph-api-test",
          message: "Use the !msgraph-api-test command instead"
      },
      "Microsoft Graph User": {
          command: "msgraph-user-test",
          message: "run the !msgraph-user-test command"
      },
      "Gmail Single User": {
          command: "gmail-auth-test",
          message: "Test is not supported."
      },
      "Microsoft 365 Defender": {
          command: "microsoft-365-defender-auth-test",
          message: "run the !microsoft-365-defender-auth-test"
      }
  };

  Object.keys(all).forEach(function(m) {

      // ✅ If specific instance is provided, skip others
      if (targetInstance && m !== targetInstance) {
          return;
      }

      var isShouldBeTesting = all[m].defaultIgnored !== 'true' &&
          INTERNAL_MODULES_BRANDS.indexOf(all[m].brand) === -1;

      if (all[m].state === 'active' && isShouldBeTesting) {

          var cmd = m.replace(/\s/g,'_') + '-test-module';

          executeCommand("addEntries", {
              entries: JSON.stringify([{
                  Type: entryTypes.note,
                  Contents: 'testing **' + m + '**',
                  HumanReadable: 'testing **' + m + '**',
                  ContentsFormat: formats.markdown
              }])
          });

          var res = executeCommand(cmd, {});
          var content = res[0].Contents || '';
          var brand = all[m].brand;
          var config = brandConfig[brand];

          // ✅ Handle special integrations
          if (config && content.includes(config.message)) {
              logDebug(`Enhanced test logic for brand "${brand}", instance "${m}"`);
              cmd = config.command;
              res = executeCommand(cmd, { using: m });
          }

          executeCommand("addEntries", {
              entries: JSON.stringify([{
                  Type: entryTypes.note,
                  Contents: 'done testing **' + m + '**:\n' + res[0].Contents,
                  HumanReadable: 'done testing **' + m + '**:\n' + res[0].Contents,
                  ContentsFormat: formats.markdown
              }])
          });

          if (res[0].Type === entryTypes.error) {
              countFailed++;
              failedInstances.push({
                  instance: m,
                  brand: all[m].brand,
                  category: all[m].category,
                  information: res[0].Contents,
                  status: 'failure'
              });
          } else {
              countSuccess++;
              instances.push({
                  instance: m,
                  brand: all[m].brand,
                  category: all[m].category,
                  information: 'succeed',
                  status: 'success'
              });
          }

      } else if (all[m].state === 'error' && isShouldBeTesting) {

          var errorMessage = 'The instance is in an error state, potentially due to an issue with the engine.';

          executeCommand("addEntries", {
              entries: JSON.stringify([{
                  Type: entryTypes.note,
                  Contents: 'done testing **' + m + '**:\n' + errorMessage,
                  HumanReadable: 'done testing **' + m + '**:\n' + errorMessage,
                  ContentsFormat: formats.markdown
              }])
          });

          countFailed++;
          failedInstances.push({
              instance: m,
              brand: all[m].brand,
              category: all[m].category,
              information: errorMessage,
              status: 'failure'
          });
      }
  });

  // ✅ If user asked for a specific instance but none matched
  if (targetInstance && countFailed === 0 && countSuccess === 0) {
      return {
          Type: entryTypes.error,
          Contents: `Instance "${targetInstance}" not found or not eligible for testing.`
      };
  }

  // ✅ Summary
  var hr;
  var success = countSuccess.toString();
  var failed = countFailed.toString();
  var total = (countSuccess + countFailed).toString();

  if (countFailed === 0) {
      failedInstances.push({});
      hr = '### All active instances are available! ✅';
  } else {
      hr = 'Total instances: ' + total + '\n';
      hr += 'Successful Instances: ' + success + '\n';
      hr += 'Failed Instances: ' + failed + '\n';
      hr += tableToMarkdown('Failed Instances:', failedInstances, ['instance', 'brand', 'category', 'information']);
  }

  return {
      Type: entryTypes.note,
      Contents: failedInstances,
      ContentsFormat: formats.markdown,
      HumanReadable: hr,
      EntryContext: {
          FailedInstances: failedInstances,
          SuccessInstances: instances,
          InstancesCount: {
              FailedCount: failed,
              SuccessCount: success,
              TotalCount: total
          }
      }
  };
scripttarget: 0
tags: []
timeout: 10.8µs
type: javascript
